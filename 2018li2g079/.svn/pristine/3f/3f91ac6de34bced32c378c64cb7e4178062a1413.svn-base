#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "cgi.h"
#include "estado.h"

/*
//Função que retorna à primeira jogada da última âncora.
void goBackAncora(ESTADO e){
	int x = e.anc.val;
	int y = e.anc.sp[x];
	int z = e.ste.sp;

	while (y != z) {
		goBack (e);
		z--;
	}
	
	e.anc.val --;
}

//Função que adiciona a jogada da stack que foi selecionada como ancora.
void setAncora(ESTADO *e){
	int x = e.ste.sp;
	int y = e.anc.val;
	y++;
	e.anc.sp[y] = x;
}
*/

